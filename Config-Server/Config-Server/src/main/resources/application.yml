server:
    port: 8085
spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/beastking21/microservices_config
          clone-on-start: true
          search-paths: '{application}'